name: tests
on: push
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
    - name: linter and cover
      uses: actions/setup-node@v2
        with:
          node-version: '8.x'
      run: npm install
      run: npm test
    